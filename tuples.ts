// maintain the order of array with tuples

let user : [string, number, boolean]

user = ["ah", 123, false]

// add tuples into "type"

type User = [number, string]

const newUser: User = [122, "Afnan Hussain"]

export{}