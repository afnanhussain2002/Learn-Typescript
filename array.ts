//  only add number in an array
const data: number[] = [1,4,5]

// only add string in an array

const data2: string [] = ["Apple", "Banana", "Orange"]

// add mix data type on an array

const data3:(string | number | boolean)[] =[1, "Apple", true]

export{}